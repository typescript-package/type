import { Primitive } from './primitive.type';
/**
 * @description The main types consist of `Function`, `object`, and `Primitive`.
 * @export
 * @typedef {Type}
 */
export type Type = Function | object | Primitive;
